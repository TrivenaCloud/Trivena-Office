/**
 * System font location moved to @trivoffice/font-metrics (shared with the docs
 * metrics pipeline); re-exported here to keep pdf-main import paths stable.
 */
export { findSystemFont, isTruetype } from '@trivoffice/font-metrics'
